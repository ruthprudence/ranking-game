// useInputPage.js
export const useInputPage = (setItems, goToMatchupPage) => {
    const handleItemChange = (index, newItem) => {
      // Logic to update items
    };
  
    const handleAddItem = () => {
      // Logic to add a new item
    };
  
    const handleRemoveItem = (index) => {
      // Logic to remove an item
    };
  
    const handleSubmit = () => {
      // Validate items and transition to matchup page
    };
  
    return { handleItemChange, handleAddItem, handleRemoveItem, handleSubmit };
  };
  